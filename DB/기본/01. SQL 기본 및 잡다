SQL Syntax(SQL 구문)
세미콜론(;)은 쿼리의 일부가 아니며 쿼리를 구분하는데 사용한다. 예를 들어, 두 개의 SQL 문을 실행하는 경우 ;을 사용해서 구분해야 한다.
SQL은 대소문자를 구분하지 않는다. 따라서 SELECT와 select는 같다.
SQL 키워드에는 대문자를 사용하고 테이블과 칼럼의 이름과 같은 식별자에는 소문자를 사용하는 룰을 사용하고 이 룰이 SQL을 더 일기 쉽게 한다.

출처 : https://www.sqltutorial.org/

샘플 DB


SQL 실행 순서
1. FROM
JOIN이 포함된 경우 JOIN을 수행한 뒤에 FROM을 수행한다. 기술적으로 JOIN 연산은 FROM 절의 일부.
2. WHERE
3. GROUP BY
4. HAVING
5. SELECT
6. DISTINCT
7. ORDER BY
8. LIMIT, OFFSET

출처 : https://www.geeksforgeeks.org/order-of-execution-of-sql-queries/
