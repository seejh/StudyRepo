# 게임 서버에서 로그인 서버를 분리하는 이유

로그인이 폭주하면 인게임에서 플레이하는 유저들이 영향을 안 받을 수 없기에 로그인을 분리.<br/>
그리고 로그인은 DB 조회나 암호화 등의 비용이 크기 때문에 이를 분리.<br/>
부가적으로는 유지보수 측면이 있다. (로그인 트래픽 증가 시 로그인 서버만 늘리면 됨)<br/>

### 인증 토큰 방식
1. 클라 -> 로그인 서버: 아이디/패스워드 전송
2. 로그인 서버 -> 클라: 임시 토큰 발급
3. 클라 -> 게임 서버: 토큰으로 접속
4. 게임 서버 -> 로그인 서버: 토큰 검증
5. 검증 완료, 게임 시작


# 로그인 서버 구현
게임 서버에서 직접 토큰을 검증하거나 로그인 서버에 토큰 검증을 요청하는 방법 등이 있다.
## 1. 게임 서버에서 직접 토큰 검증 방법.
### 프로젝트 구조, 필요 NuGet 패키지, DB 테이블, appsettings.json
```
// 프로젝트 구조
LoginServer
Program.cs
- Controllers
 - AuthController.cs
- Services
 - AuthService.cs
- Models
 - User.cs

// 필요 Nuget 패키지
dotnet add package MySql.Data
dotnet add package BCrypt.Net-Next
dotnet add package Microsoft.AspNetCore.Authentication.JwtBearer
dotnet add package System.IdentityModel.Tokens.Jwt

// DB 테이블
CREATE TABLE users (
 id int primary key auto_increment,
 user_id varchar(50) unique not null,
 password_hash varchar(255) not null,
 created_at datetime default now()
)

// appsettings.json
{
  "ConnectionStrings": {
    "MySQL": "Server=localhost;Database=gamedb;User=root;Password=yourpassword;"
  },
  "Jwt": {
    "SecretKey": "your-256-bit-secret-key-here-make-it-long",
    "ExpiresInSeconds": 60
  },
  "GameServer": {
    "Ip": "127.0.0.1",
    "Port": 9000
  }
}
```
### User.cs
```
public class User
{
    public int Id { get; set; }
    public string UserId { get; set; }
    public string PasswordHash { get; set; }
}

public class LoginRequest
{
    public string Id { get; set; }
    public string Password { get; set; }
}

public class LoginResponse
{
    public string Token { get; set; }
    public string GameServerIp { get; set; }
    public int GameServerPort { get; set; }
}
```
### AuthService.cs
```
using MySql.Data.MySqlClient;
using Microsoft.IdentityModel.Tokens;
using System.IdentityModel.Tokens.Jwt;
using System.Security.Claims;
using System.Text;
using BCrypt.Net;

public class AuthService
{
    private readonly IConfiguration _config;
    private readonly string _connStr;

    public AuthService(IConfiguration config)
    {
        _config = config;
        _connStr = config.GetConnectionString("MySQL");
    }

    public async Task<User?> GetUserAsync(string userId)
    {
        using var conn = new MySqlConnection(_connStr);
        await conn.OpenAsync();

        var cmd = new MySqlCommand(
            "SELECT id, user_id, password_hash FROM users WHERE user_id = @userId", conn);
        cmd.Parameters.AddWithValue("@userId", userId);

        using var reader = await cmd.ExecuteReaderAsync();
        if (!await reader.ReadAsync()) return null;

        return new User
        {
            Id = reader.GetInt32("id"),
            UserId = reader.GetString("user_id"),
            PasswordHash = reader.GetString("password_hash")
        };
    }

    public bool VerifyPassword(string password, string hash)
    {
        // bcrypt로 비밀번호 검증
        return BCrypt.Net.BCrypt.Verify(password, hash);
    }

    public string GenerateToken(int userId)
    {
        var secretKey = _config["Jwt:SecretKey"];
        var expiresIn = int.Parse(_config["Jwt:ExpiresInSeconds"]);

        var key = new SymmetricSecurityKey(Encoding.UTF8.GetBytes(secretKey));
        var creds = new SigningCredentials(key, SecurityAlgorithms.HmacSha256);

        var token = new JwtSecurityToken(
            claims: new[] { new Claim("userId", userId.ToString()) },
            expires: DateTime.UtcNow.AddSeconds(expiresIn),
            signingCredentials: creds
        );

        return new JwtSecurityTokenHandler().WriteToken(token);
    }
}
```
### AuthController.cs
```
using Microsoft.AspNetCore.Mvc;

[ApiController]
[Route("[controller]")]
public class AuthController : ControllerBase
{
    private readonly AuthService _authService;
    private readonly IConfiguration _config;

    public AuthController(AuthService authService, IConfiguration config)
    {
        _authService = authService;
        _config = config;
    }

    [HttpPost("login")]
    public async Task<IActionResult> Login([FromBody] LoginRequest request)
    {
        // 1. 유저 조회
        var user = await _authService.GetUserAsync(request.Id);
        if (user == null)
            return Unauthorized(new { error = "아이디가 없습니다" });

        // 2. 비밀번호 검증
        if (!_authService.VerifyPassword(request.Password, user.PasswordHash))
            return Unauthorized(new { error = "비밀번호가 틀렸습니다" });

        // 3. 토큰 발급
        var token = _authService.GenerateToken(user.Id);

        return Ok(new LoginResponse
        {
            Token = token,
            GameServerIp = _config["GameServer:Ip"],
            GameServerPort = int.Parse(_config["GameServer:Port"])
        });
    }
}
```
### Program.cs
```
using Microsoft.AspNetCore.Builder;
using Microsoft.Extensions.DependencyInjection;

var builder = WebApplication.CreateBuilder(args);

builder.Services.AddControllers();
builder.Services.AddSingleton<AuthService>();

var app = builder.Build();

app.UseHttpsRedirection();
app.MapControllers();
app.Run();
```
### c++ 게임 서버
```
#include <jwt-cpp/jwt.h>

void onLoginPacket(Session* session, std::string token) {
    try {
        auto decoded = jwt::decode(token);
        auto verifier = jwt::verify()
            .allow_algorithm(jwt::algorithm::hs256{"your-256-bit-secret-key-here-make-it-long"})
            .with_claim("userId", jwt::claim(std::string{}));

        verifier.verify(decoded);

        // 만료 시간 확인
        auto exp = decoded.get_expires_at();
        if (exp < std::chrono::system_clock::now()) {
            session->disconnect();
            return;
        }

        int userId = std::stoi(decoded.get_payload_claim("userId").as_string());
        loadPlayerData(session, userId);

    } catch (const std::exception& e) {
        // 토큰 검증 실패
        session->disconnect();
    }
}
```

## 2. 로그인 서버에 검증 요청 방법
구글 OAuth 같은, 로그인 검증을 외부에 맡길 때<br/>

### 작동
* 클라 -> 로그인 서버(구글)
  * https post 로그인 요청(id, pw)

* 로그인 서버
  * 해당 유저가 존재하는지 db에서 확인(mysql)
  * 비밀번호 검증(bcrypt)
  * 정당할 경우 토큰 생성 후 redis에 저장
  * 토큰과 서버 정보를 담아서 클라에게 응답

* 클라 -> 게임 서버
  * 받은 서버 정보로 tcp 소켓 접속
  * 로그인 요청(토큰 제시)

* 게임 서버 -> 로그인 서버
  * 토큰 검증 요청
 
* 로그인 서버 -> 게임 서버
  * 토큰 검증 확인
 
* 게임 서버 -> 클라
  * 로그인 허용

### 필요 NuGet 패키지, appsettings.json
```
dotnet add package StackExchange.Redis

{
  "ConnectionStrings": {
    "MySQL": "Server=localhost;Database=gamedb;User=root;Password=yourpassword;",
    "Redis": "localhost:6379"
  },
  "GameServer": {
    "Ip": "127.0.0.1",
    "Port": 9000
  },
  "Token": {
    "ExpiresInSeconds": 60
  }
}
```
### Services/TokenService.cs
```
using StackExchange.Redis;

namespace LoginServer.Services
{
    public class TokenService
    {
        private readonly IDatabase _redis;
        private readonly int _expiresIn;

        public TokenService(IConfiguration config)
        {
            var redis = ConnectionMultiplexer.Connect(config.GetConnectionString("Redis"));
            _redis = redis.GetDatabase();
            _expiresIn = int.Parse(config["Token:ExpiresInSeconds"]);
        }

        // 토큰 생성 후 Redis에 저장
        public async Task<string> CreateTokenAsync(int userId)
        {
            // 랜덤 토큰 생성
            var token = Guid.NewGuid().ToString("N");  // "N": 하이픈 없는 32자리

            // Redis에 저장 (key: token, value: userId, TTL: 60초)
            await _redis.StringSetAsync(
                $"token:{token}",
                userId.ToString(),
                TimeSpan.FromSeconds(_expiresIn)
            );

            return token;
        }

        // 토큰 검증 (게임 서버에서 호출)
        public async Task<int?> ValidateTokenAsync(string token)
        {
            var value = await _redis.StringGetAsync($"token:{token}");

            if (value.IsNullOrEmpty) return null;

            // 검증 후 즉시 삭제 (1회용 토큰)
            await _redis.KeyDeleteAsync($"token:{token}");

            return int.Parse(value);
        }
    }
}
```
### Services/AuthService.cs
```
using MySql.Data.MySqlClient;
using BCrypt.Net;

namespace LoginServer.Services
{
    public class AuthService
    {
        private readonly string _connStr;

        public AuthService(IConfiguration config)
        {
            _connStr = config.GetConnectionString("MySQL");
        }

        public async Task<User?> GetUserAsync(string userId)
        {
            using var conn = new MySqlConnection(_connStr);
            await conn.OpenAsync();

            var cmd = new MySqlCommand(
                "SELECT id, user_id, password_hash FROM users WHERE user_id = @userId", conn);
            cmd.Parameters.AddWithValue("@userId", userId);

            using var reader = await cmd.ExecuteReaderAsync();
            if (!await reader.ReadAsync()) return null;

            return new User
            {
                Id = reader.GetInt32(0),
                UserId = reader.GetString(1),
                PasswordHash = reader.GetString(2)
            };
        }

        public bool VerifyPassword(string password, string hash)
        {
            return BCrypt.Net.BCrypt.Verify(password, hash);
        }
    }
}
```
### Controllers/AuthController.cs 
```
using Microsoft.AspNetCore.Mvc;
using LoginServer.Services;
using LoginServer.Models;

namespace LoginServer.Controllers
{
    [ApiController]
    [Route("[controller]")]
    public class AuthController : ControllerBase
    {
        private readonly AuthService _authService;
        private readonly TokenService _tokenService;
        private readonly IConfiguration _config;

        public AuthController(AuthService authService, TokenService tokenService, IConfiguration config)
        {
            _authService = authService;
            _tokenService = tokenService;
            _config = config;
        }

        // 클라이언트 로그인
        [HttpPost("login")]
        public async Task<IActionResult> Login([FromBody] LoginRequest request)
        {
            // 1. 유저 조회
            var user = await _authService.GetUserAsync(request.Id);
            if (user == null)
                return Unauthorized(new { error = "아이디가 없습니다" });

            // 2. 비밀번호 검증
            if (!_authService.VerifyPassword(request.Password, user.PasswordHash))
                return Unauthorized(new { error = "비밀번호가 틀렸습니다" });

            // 3. 토큰 생성 후 Redis에 저장
            var token = await _tokenService.CreateTokenAsync(user.Id);

            return Ok(new LoginResponse
            {
                Token = token,
                GameServerIp = _config["GameServer:Ip"],
                GameServerPort = int.Parse(_config["GameServer:Port"])
            });
        }

        // 게임 서버에서 토큰 검증 요청
        [HttpPost("verify")]
        public async Task<IActionResult> Verify([FromBody] VerifyRequest request)
        {
            var userId = await _tokenService.ValidateTokenAsync(request.Token);

            if (userId == null)
                return Unauthorized(new { error = "유효하지 않은 토큰" });

            return Ok(new { userId });
        }
    }
}
```
### Models/User.cs
```
namespace LoginServer.Models
{
    public class User
    {
        public int Id { get; set; }
        public string UserId { get; set; }
        public string PasswordHash { get; set; }
    }

    public class LoginRequest
    {
        public string Id { get; set; }
        public string Password { get; set; }
    }

    public class LoginResponse
    {
        public string Token { get; set; }
        public string GameServerIp { get; set; }
        public int GameServerPort { get; set; }
    }

    // 추가
    public class VerifyRequest
    {
        public string Token { get; set; }
    }
}
```
### Program.cs
```
using LoginServer.Services;

var builder = WebApplication.CreateBuilder(args);

builder.Services.AddControllers();
builder.Services.AddSingleton<AuthService>();
builder.Services.AddSingleton<TokenService>();  // 추가

var app = builder.Build();

app.UseHttpsRedirection();
app.MapControllers();
app.Run();
```
### 게임 서버
```
// 클라이언트가 접속 시 토큰 전송
void onLoginPacket(Session* session, std::string token) {
    // 로그인 서버에 검증 요청 (HTTP POST)
    httpClient.post("http://login-server/auth/verify",
        { "token": token },
        [session](bool success, int userId) {
            if (!success) {
                session->disconnect();
                return;
            }
            loadPlayerData(session, userId);
        }
    );
}
```




